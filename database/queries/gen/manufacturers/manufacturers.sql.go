// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: manufacturers.sql

package manufacturers

import (
	"context"
)

const countByOrgID = `-- name: CountByOrgID :one
SELECT COUNT(*) FROM manufacturers
WHERE org_id = ?
`

func (q *Queries) CountByOrgID(ctx context.Context, orgID string) (int64, error) {
	row := q.db.QueryRowContext(ctx, countByOrgID, orgID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const create = `-- name: Create :one
INSERT INTO manufacturers (
    id,
    org_id,
    name
) VALUES (
    ?,
    ?,
    ?
) RETURNING
    id,
    org_id,
    name,
    created_at
`

type CreateParams struct {
	ID    string
	OrgID string
	Name  string
}

type CreateRow struct {
	ID        string
	OrgID     string
	Name      string
	CreatedAt int64
}

func (q *Queries) Create(ctx context.Context, arg CreateParams) (CreateRow, error) {
	row := q.db.QueryRowContext(ctx, create, arg.ID, arg.OrgID, arg.Name)
	var i CreateRow
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Name,
		&i.CreatedAt,
	)
	return i, err
}

const delete = `-- name: Delete :exec
DELETE FROM manufacturers
WHERE id = ?
`

func (q *Queries) Delete(ctx context.Context, id string) error {
	_, err := q.db.ExecContext(ctx, delete, id)
	return err
}

const getByID = `-- name: GetByID :one
SELECT
    id,
    org_id,
    name,
    updated_at,
    created_at
FROM manufacturers
WHERE id = ?
`

func (q *Queries) GetByID(ctx context.Context, id string) (Manufacturer, error) {
	row := q.db.QueryRowContext(ctx, getByID, id)
	var i Manufacturer
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Name,
		&i.UpdatedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getByOrgID = `-- name: GetByOrgID :many
SELECT
    id,
    org_id,
    name,
    updated_at,
    created_at
FROM manufacturers
WHERE org_id = ?
`

func (q *Queries) GetByOrgID(ctx context.Context, orgID string) ([]Manufacturer, error) {
	rows, err := q.db.QueryContext(ctx, getByOrgID, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Manufacturer
	for rows.Next() {
		var i Manufacturer
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.Name,
			&i.UpdatedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const update = `-- name: Update :one
UPDATE manufacturers
SET
    name = ?,
    updated_at = unixepoch()
WHERE id = ?
RETURNING
    id,
    org_id,
    name,
    updated_at,
    created_at
`

type UpdateParams struct {
	Name string
	ID   string
}

func (q *Queries) Update(ctx context.Context, arg UpdateParams) (Manufacturer, error) {
	row := q.db.QueryRowContext(ctx, update, arg.Name, arg.ID)
	var i Manufacturer
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.Name,
		&i.UpdatedAt,
		&i.CreatedAt,
	)
	return i, err
}
