{{ define "title" }}Error{{ end }}
{{ define "description" }}Something went wrong. Please try again later.{{ end }}

{{ define "page" }}
<div class="text-center">
  <h1>Error</h1>
  <p>{{ if .Message }}{{ .Message }}{{ else }}Something went wrong. Please try again later.{{ end }}</p>
  {{ if .TraceID }}<p class="text-muted"><small>Trace ID: {{ .TraceID }}</small></p>{{ end }}
  <br>
  <a href="/" class="btn btn-primary">Go to Home</a>
</div>
{{ end }}
